// Skeleton shown while the GitHub stars badge is loading (Suspense fallback)
export default function GitHubStarsLoading() {
  return (
    <div
      aria-hidden="true"
      className="fixed bottom-6 right-6 z-50 h-8 w-20 animate-pulse rounded-full bg-[--muted]"
    />
  );
}
